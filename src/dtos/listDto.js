class ListDTO {
    constructor({
        region,
        district,
        features,
    }) {
        this.region = region;
        this.district = district;
        this.features = features;
    }
}

export default ListDTO;